# TODO: Remove this comment once this file has been proofread

_    Activates the IOAO SPRITES FROM WE facility.
ISVMBOL Place tape m your cassette recorder. Press SYMBOL SHIFT fJdata and press PLAY on the cassette recorder. Three groups SHIFT gHIFT    will load . When loaded, the Text Line will clear and the program will resume.
N7fE: Any sprites in cnyrory will be destroyed when this command is executed.

5    Move CHR$ SQR 1 place left

Activates the MOVE CHRS (;OR.qOR 1 place to the left - nom

6    Move CHR$ SQR 1 place down

6
destructive.
Activates the MOVE CFRS `W CUP_ I place down - non-destructive.

7    Move CHR$ SQR 1 place up

Activates the M~E CIjR$ SQR cLASOR 1 place up - non
destructive

8    Move CHR$ SQR 1 place right

Activates the MOVE cHRS S~ a1R90R 1 place to the right -

9    Set CHR$ SQR at current position

non-destructive.
g    Activates the SEP CHRS at wrrent position.

0    Clear CHR$ SQR at current position

Activates the CIkAR QTRS `.+QR at ceiTent Pceition.
q{,    Activates the MUVE SPRITE 9::R~ CLF-qOR 1 place to the left.
ISVMBOL SHIFT
61
9    Activates the MOVE SPRI'iE S~REFN CUWR 1 place down•


ISYMBOL SHIFT
81


pctivate-s the PDVE SPRM't: 9CREE2J L'OR~ 1 Place up.
(SYMBOL SHIFT 7)
(    Activates the NDV2 SPRl'TS 9CRp'FN clmMR 1 place to t}ie right.


(BYMBOL SHIFT BI
(    Activates the REtOCATE SRRl'Lfs fl'ncdatatren about jn ~n,ory.
IBYMBOL puws the user to move the sprite
SHIFT    hetween the top of the Sprite Ornerator Program and addcess
RI    65520.


e.g. a positive numUer i.e. 50, moves the data 50 bytes uD in me+norY• A negativemamber i.e. -50, rtoves the data 50

bytes doan >,r, mmnry.
CALTtOpl _ use this function with care.

BREAK + SPACE    Place Sprite into Sprite Window function
    Places a smaller sprite into a larger one at position ROW,COL in
    memory. There are four options:
    1 = GETBLS; GETs the smaller sprite data into the larger sprite window
    2 = GETORS;  ORs the smaller sprite data into the larger sprite window
    3 = GETXRS; XORs the smaller sprite data into the larger sprite window
    4 = GETNDS; ANDs the smaller sprite data into the larger sprite window

BREAK Activates the pi.PCE SPR11£ INN SPRPIE WINDOW function.

and    ~is allows you to Place a sprite °f smaller dimensions into

SPACE    a~ sprite of greater dimensrons, at a Position of ROW'

in the yreater sprite >.n mmnn, _ the s"aller sprite is
OpL
left unaltered.